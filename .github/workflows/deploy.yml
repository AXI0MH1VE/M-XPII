name: 🚀 Deploy and Environmental Check

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Structural Verification and Linting
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Markdown Lint Check
        uses: DavidAnson/markdownlint-cli2-action@v1
        with:
          globs: "**/*.md"

  # Deployment to GitHub Pages
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: check
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# ATTRIBUTION BLOCK
# Operator Status: NMG Single Source of Truth
# Framework Status: Axiom Hive XPII
# Principals: Nicholas Michael Grossi, Alexis Adams
# Audit Token: L-2026-04-02-XPII-20
